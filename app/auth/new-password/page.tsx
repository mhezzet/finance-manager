import { NewPasswordForm } from '@/components/auth/new-passowrd-form';
import { NextPage } from 'next';

const NewPasswordPage: NextPage = ({}) => {
  return <NewPasswordForm />;
};

export default NewPasswordPage;
